numbers = [-1, 2, 4, 93, 67, -22, 999, 293]

def even_numbers(numbers):
    counter = 0
    for number in numbers:
        if number % 2 == 0:
            counter += 1

    return counter

print(f"there are {(even_numbers(numbers))} even numbers")

